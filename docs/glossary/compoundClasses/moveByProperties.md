```css
// To use the `applyProperties` method instead of the `animate` method when dragging objects using the Purge TSS Animation Module.
'.move-by-properties': { moveByProperties: true }
'.move-by-properties-false': { moveByProperties: false }
```
